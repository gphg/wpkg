# This file is the registry for remote url (prefix). This shorten the url by registry name.
# Each line is one remote register separated by tab. The format to adhere to is:
#
# <REGISTRY_NAME>	<REGISTRY_REMOTE_URL>
#
# Example:
# github	https://github.com/
#
# Script will look up the current script: wpkg.sh (global default), then merged by one on $CWD (local project).
#
github	https://github.com/
gitlab	https://gitlab.com/
android	https://dl.google.com/android/repository/
# Same as github, but a specific user/org
noto	https://github.com/notofonts/
